// This configuration file holds settings for deploying a Windows 2022 template.

{
    // The IP address or hostname of your vSphere server
    "vsphere-server": "vSphere IP Address", 
    
    // The vSphere user (often "administrator@vsphere.local")
    "vsphere-user": "administrator@vsphere.local",
    
    // The password for the specified vSphere user
    "vsphere-password": "vSphere user Password",
    
    // The name of your vSphere datacenter
    "vsphere-datacenter": "Datacenter",
    
    // The name of the cluster within the datacenter
    "vsphere-cluster": "cluster",
    
    // The VM network (port group) to assign to the template
    "vsphere-network": "VM Network",
    
    // The datastore where the template should reside
    "vsphere-datastore": "datastore",
    
    // The folder in vSphere where the template will be placed
    "vsphere-folder": "Folder",
    
    // The VM (template) display name
    "vm-name": "Win2022-Template-Base",
    
    // Number of virtual CPUs for the VM
    "vm-cpu-num": "2",
    
    // The amount of memory (in MB) allocated to the VM (4096 = 4GB)
    "vm-mem-size": "4096",
    
    // The total size (in MB) of the main OS disk (40960 = 40GB)
    "os-disk-size": "40960",
    
    // Whether the VM disk should be thin-provisioned ("true" or "false")
    "disk-thin-provision": "true",
    
    // The local Administrator password for the Windows template
    "winadmin-password": "P@ssw0rd",
    
    // The path to your Windows Server 2022 ISO (including datastore reference)
    "os_iso_path": "[dslocal] ISOs/SW_DVD9_Win_Server_STD_CORE_2022_2108.24_64Bit_English_DC_STD_MLF_X23-54269.ISO"
}
